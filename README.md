# SqlFolderExecuter
This is a command line tool to execute SQL scripts in a folder against a database. It is built using .NET 10.0 and C# 14.0.
The command line tool takes the following parameters: local SQL Server database name, *.sql folder path. 
